{
  "name": "cli-surface",
  "description": "Every registered command is invocable and the documented surface matches the code.",
  "tasks": [
    {
      "id": "cli-help-walk",
      "prompt": "Invoke --help on every leaf command and assert no duplicate registrations.",
      "commands": ["python -m pytest tests/unit/test_cli_surface.py -q"],
      "success": ["command:0"]
    },
    {
      "id": "docs-drift",
      "prompt": "The packaged command reference must match the live CLI tree.",
      "commands": ["magent docs generate-reference --check"],
      "success": ["command:0"]
    }
  ]
}
